# LLM
anthropic>=0.40.0

# Vector store (long-term memory)
chromadb>=0.5.0

# Web search (no API key required)
duckduckgo-search>=6.0.0

# HTTP client + HTML parser (optional depth fetch)
httpx>=0.27.0
beautifulsoup4>=4.12.0

# Config & CLI
python-dotenv>=1.0.0
rich>=13.0.0

# Testing
pytest>=8.0.0
